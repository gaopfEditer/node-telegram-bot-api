# Telegram Bot API 测试环境变量配置
# 复制此文件为 .env 并填写你的实际值
# 或者直接在命令行中设置这些环境变量

# 必需：Telegram Bot Token（从 @BotFather 获取）
TEST_TELEGRAM_TOKEN=your_bot_token_here

# 必需：测试用户 ID（用于发送消息）
# 可以通过 @userinfobot 获取你的用户 ID
TEST_USER_ID=your_user_id_here

# 必需：测试群组 ID（用于群组相关测试）
# 将机器人添加到群组后，可以通过 @getidsbot 获取群组 ID
# 注意：群组 ID 通常是负数，例如：-1001234567890
TEST_GROUP_ID=your_group_id_here

# 可选：支付提供商 Token（用于发票相关测试）
# 如果不需要测试支付功能，可以留空（在非 CI 环境下会报错但可以跳过相关测试）
TEST_PROVIDER_TOKEN=your_provider_token_here

# 可选：游戏短名称（默认：medusalab_test）
TEST_GAME_SHORT_NAME=medusalab_test

# 可选：贴纸集名称（默认：pusheen）
TEST_STICKER_SET_NAME=pusheen

# ============================================
# 监听群组消息脚本配置（listen-group-messages.js）
# ============================================
# 可选：目标群组 ID（不设置则监听所有群组）
# TARGET_GROUP_ID=-1001234567890

# 可选：目标用户 ID，多个用逗号分隔（不设置则监听所有用户）
# TARGET_USER_IDS=123456789,987654321

